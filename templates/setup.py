from setuptools import setup

setup(
    name="",
    version="21.00.00",  # year.month.day
    packages=["pkgname"],
    package_dir={"pkgname": "src"},
    install_requires=[
        "python-dotenv",
    ],
)
